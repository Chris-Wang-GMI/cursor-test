# React TypeScript Library Skeleton

This project provides a minimal starting point for creating a TypeScript library
using **React**, **Tailwind CSS** for styling and `shadcn/ui` components.

## Development

```bash
npm install
npm run build
```

The compiled files will be located in the `dist` directory.

